---
topic: Wave 4 harness surgery: primary-stack-safe validation + BMad-only harness (pre-v0.13 gate)
updated: 2026-08-05T04:33
---

- (event) Create operation, express mode: input = bmad-help session findings (culprit lines pinpointed) + PRD prd-imoveis-2026-08-05 §6.2 harness track + ADR 0005 + project-context.md.
- (direction) Felipe: harness surgery must land BEFORE readiness validation and sprint planning; primary local DB was being reset by agent scripts; keep all local dev containers intact; clean unneeded harness/docs steps; re-evaluate harness around BMad.
- (constraint) Primary compose project `imoveis` is inviolable: no validation/finishing/cleanup script may create, recreate, restart, stop, or down its containers, nor mutate `realestate` schema or data. Root cause: validate.sh:147 `up -d postgres redis` on primary + alembic upgrade on primary (validate.sh:149,164,186).
- (decision) Validation moves to an ephemeral compose project `imoveis-test`: own ports, throwaway volume, image parity (PostGIS 17-3.5 + pgvector); alembic during validation runs only there. ensure-test-db.sh DB-level isolation is insufficient — isolation must be container-level.
- (decision) Primary `realestate` migration leaves validate.sh entirely and becomes an explicit operator step; mechanism deliberately open (OQ-1).
- (constraint) Destructive docker ops fail closed: teardown/docker-cleanup must refuse when compose project identity is ambiguous or resolves to the primary project (teardown.sh:62 guard currently trusts COMPOSE_PROJECT_NAME env alone).
- (capability) CAP-1 intent: operator/agent runs validate.sh (fast, backend, all) at any time, including during a live backfill, without touching the primary stack. success: `validate.sh all` completes with zero container create/recreate/restart/stop events on compose project `imoveis` and identical pre/post dump of `realestate` schema+rowcounts.
- (capability) CAP-2 intent: finishing/teardown/cleanup cannot destroy primary state even under env drift. success: teardown invoked with unset and with wrong COMPOSE_PROJECT_NAME refuses destructive action against the primary project (drift simulation test).
- (capability) CAP-3 intent: harness skill surface reduced to BMad + bmad-loop — delete feature-pipeline, babysit-pr, code-review, epic-completion, harness-retrospect, imoveis-planning-bridge, security-scan from .claude/skills; still-needed behavior absorbed per dispositions table. success: none of the 7 remain; each absorbed behavior reachable via its named target.
- (capability) CAP-4 intent: CLAUDE.md (project) rewritten around BMad-SoR + bmad-loop + scripts/agent gates. success: no feature-pipeline mandate, no BMad-dev-skill prohibition, all gates (validate.sh, finish gate, docker-cleanup, domain hooks, epic-completion re-read discipline) preserved as mandatory, zero dangling references to deleted skills.
- (capability) CAP-5 intent: living guidance docs pruned of retired-regime instructions (harness-troubleshooting.md, development-guide.md, deployment-guide.md, ADR living references, source-tree-analysis.md). success: no living doc instructs feature-pipeline/Linear-era flow; legacy BIN-* feature docs and chats_history untouched.
- (capability) CAP-6 intent: BMad dev skills constrained via _bmad/custom overrides (bmad-customize) so any dev loop calls scripts/agent gates. success: overrides exist for bmad-dev-story/bmad-quick-dev/bmad-dev-auto requiring validate.sh, forbidding raw pytest and any compose action on the primary project.
- (capability) CAP-7 intent: project-context.md regenerated post-rewrite. success: _bmad-output/project-context.md reflects the new regime — rules at current lines 82 (validate recreates primary) and 87 (BMad dev skills must not replace pipeline) replaced by the new invariants.
- (constraint) scripts/agent/ stays the enforcement layer (ADR 0002): BMad skills call the gates, never reimplement or replace them; domain hooks (validate-scrapers --require-live, validate-ai, contract tests, alembic check) survive unweakened.
- (constraint) Surgery implementation must bootstrap-validate on the new imoveis-test stack — it may not run the old primary-touching validate path even once during its own delivery.
- (constraint) Never any named-volume deletion or `docker system prune --volumes` anywhere in the reworked scripts.
- (note) Non-goals: product code (src/, frontend/) and CI workflow changes beyond gate wiring; renaming/migrating legacy BIN-* docs; Wave 3 per-epic verification; building/altering bmad-loop internals; pruning WDS/CIS BMad modules; any change to primary data.
- (assumption) finish-feature.sh (or a renamed successor) survives as the push/PR/merge/cleanup gate under bmad-loop — the skill feature-pipeline dies, the script gate does not.
- (assumption) Ephemeral project name `imoveis-test` (Felipe's naming); collision-free ports derived, not hardcoded.
- (assumption) Surgery is documented under a v0.13-fu<N> key per feature-doc convention (PRD calls the harness track process, not FR; sprint planning has not yet minted keys).
- (question) OQ-1: where does primary `realestate` migration run post-surgery — dedicated scripts/agent/migrate-primary.sh, run-services.sh startup, or manual step documented in CLAUDE.md?
- (question) OQ-2: which target absorbs babysit-pr's CI-watch/merge behavior — finish-feature.sh itself, a bmad-customize override on the dev skills, or bmad-loop's story cycle?
- (question) OQ-3: does the surgery also rewrite the Cursor mirror (.cursor/rules, .cursor/skills) and global ~/.claude/CLAUDE.md references, or project files only this pass?
- (note) Companions spawned: brownfield.md (current-state findings, culprit lines) + skill-dispositions.md (7-skill disposition matrix, docs pruning targets, CLAUDE.md inversion list).
- (note) Adopted companion: _bmad-output/project-context.md (GPC-owned; CAP-7 regenerates it).
- (event) Self-validate pass 1 (coherence): PASS — 7 capabilities all carry intent+success; constraints all rule something out; 6 non-goals; success signal testable (bit-identical primary + fresh-session CLAUDE.md drill); IDs CAP-1..7 fresh and unique; prose lean. Weakness noted: CAP-6 success names concrete override files (edges toward HOW) — accepted, since the override surface IS the contract.
- (event) Self-validate pass 2 (preservation): PASS — all five scope items of the input landed (invariant+culprits→Constraints/brownfield; 7-skill deletion→CAP-3/dispositions; CLAUDE.md rewrite→CAP-4/inversions; docs pruning→CAP-5/targets; customize+GPC follow-ups→CAP-6/7); PRD §6.2 gate in Why; user directives (containers intact, cleanup, BMad re-evaluation) all covered. Wrapper-only content dropped: bmad-help session routing prose (process metadata).
- (direction) Felipe: skip the PR step entirely (BMad dev workflow + local gates validate implementations); remove CI if not needed, keeping lint+spec verification at ticket-dev / sprint-epic validation time; OQ-3 = YES include Cursor mirror + global CLAUDE.md; no Linear anywhere, no CI we do not need.
- (decision) PR step retired. Finish gate reshaped fully local: validate.sh all (green mandatory) then local squash-merge to main then mandatory `git push origin main` (origin = sole backup, no longer a gate) then cleanup. Supersedes earlier assumption that finish-feature.sh keeps its PR/CI-watch path.
- (decision) OQ-2 resolved by dissolution: with no PR and no remote CI there is nothing to babysit — babysit-pr deleted with NO absorption target.
- (decision) GitHub merge-gate CI retired: ci.yml and sonar-project.properties deleted; Sonar new-code gate dropped (solo project; risk-tiered testing discipline in CLAUDE.md remains the coverage policy).
- (decision) CI-parity guardrail (new CAP-9): validate.sh absorbs `pre-commit run --all-files` — CI linted ALL files while local validate only flake8'd src/ (known drift trap). Invariant: any check that could block a merge pre-surgery must still be able to block a merge locally post-surgery.
- (decision) OQ-3 resolved YES: surgery also rewrites the Cursor mirror (.cursor/rules, .cursor/skills) and global ~/.claude/CLAUDE.md (including dropping the legacy bmad-linear-bridge reference); zero Linear references remain anywhere in harness files.
- (decision) Supersedes the non-goal excluding CI workflow changes: CI retirement is now IN scope. New non-goal: no replacement CI system (self-hosted runners, act, scheduled local cron suites) — the local gates are the only gate.
- (assumption) OQ-1 was NOT covered by PR/CI removal (it concerns primary `realestate` schema migration, which validation no longer performs). Resolved by inference: dedicated scripts/agent/migrate-primary.sh as an explicit operator step, guarded to refuse while the backfill pacer lock (`backfill:gemma`) is active. Pending Felipe's veto.
- (assumption) docs.yml (MkDocs GitHub Pages deploy) kept — it is publishing, not a gate; removing it would kill the docs site for no speed gain.
- (question) OQ-4: fates of the remaining non-gating GitHub surfaces — nightly.yml (full suite + dependency audit + live scraper drift canary) and dependabot.yml. Keep as advisory-only safety nets (bumps/drift surfaced by GitHub, validated locally before merge) or delete for a fully-GitHub-free loop? Recommendation on record: keep nightly's scrapers drift-canary job + Dependabot advisories; drop nightly full-suite and dependency-audit (redundant with local gates).
- (capability) CAP-8 intent: ticket delivery completes fully locally with no PR and no remote CI gate — finish gate = validate.sh all, local squash-merge to main, push origin main, cleanup. success: a story ships with zero GitHub PR/checks interaction; origin/main equals local main after finish; a red validate.sh all blocks the merge.
- (capability) CAP-9 intent: still-valuable retired-CI checks are absorbed into the local gates. success: ci.yml + sonar-project.properties removed; validate.sh runs pre-commit on all files; a CI-only lint violation (e.g. F401 outside src/) planted in a scratch file fails validate.sh locally.
- (event) SPEC re-derived (update): CAP-1..7 preserved, CAP-8/9 added, OQ-1 closed as assumption, OQ-2/OQ-3 closed as decisions, OQ-4 opened, non-goal on CI inverted, success signal updated to the PR-less shape.
- (event) Self-validate after update: pass 1 (coherence) PASS — CAP-8/9 carry intent+success, new constraints rule things out (no gate weakening, mandatory push), non-goal inversion recorded not silently edited, IDs stable CAP-1..9. Pass 2 (preservation) PASS — Felipe's three directives (no PR, no CI with local verification kept, OQ-3 yes incl. Cursor+global+no-Linear) all landed in kernel or companions; OQ-1 correction preserved as assumption rather than silently dropped.
- (direction) Felipe on OQ-1: concern that the migrate-primary backfill guard would need manual removal after the backfill finishes to avoid time/token cost. OQ-4: go with the recommendation.
- (decision) Guard design refined (answers the OQ-1 concern): migrate-primary.sh guard is a runtime Redis check against a TTL'd heartbeat the backfill runner refreshes while alive — absent heartbeat means the guard passes in milliseconds. Self-clearing; no manual removal, no agent reasoning, no token cost ever. Guard must key off the live heartbeat, NOT persistent pacer state (leftover backfill:gemma keys after completion must not block). migrate-primary.sh assumption confirmed as accepted.
- (decision) OQ-4 closed per recommendation: nightly.yml keeps only the scraper drift-canary job (full-suite and dependency-audit jobs deleted as redundant with local gates); dependabot.yml kept advisory-only — bumps validated locally via validate.sh before merge, no CI checks on them.
- (event) Spec finalized: zero open questions remain; all assumptions confirmed or standing unvetoed; contract ready for bmad-quick-dev execution in a fresh session.
