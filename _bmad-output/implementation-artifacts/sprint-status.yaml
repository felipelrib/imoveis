# generated: 2026-08-05T06:51:14-03:00 (bmad-sprint-planning regeneration; v0.13 wave)
# last_updated: 2026-08-05T07:20:00-03:00 (re-keyed to BMad-standard keys for bmad-loop)
# project: imoveis
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts
#
# NOTE (2026-08-05, ADR 0005): Linear DROPPED entirely. This file is the SOLE
# execution-status tracker; epics.md = plan of record (planningTarget v0.13).
# Delivered pre-v0.13 history (epics 1-8) is archived verbatim in
# sprint-status-archive-pre-v0.13.yaml — this file tracks ONLY the current
# wave, with BMad-standard keys (epic-N, N-M-slug) that bmad-loop parses.
#
# KEY MAPPING: yaml story N-M ↔ project story id v0.13-sN.M (branch names
# feat/v0.13-sN.M-…, feature docs docs/features/v0.13-sN.M-*.md — the
# finish-feature.sh doc gate derives the id from the branch). Follow-ups
# without a story keep their v0.13-fu<N> keys below (opaque to bmad-loop).
#
# SEQUENCING GATES (from epics.md frontmatter `tracking:` — enforce order):
#   1.2←1.1, 1.3←1.1, 1.4←1.1, 1.5←1.3, 1.6←1.4+1.5,
#   2.2←2.1, 2.3←2.1, 2.4←2.3, 2.5←2.2.
#   Wave 1: 1.1 ∥ 2.1 → Wave 2: 1.2 ∥ 1.3 ∥ 1.4 ∥ 2.2 ∥ 2.3 →
#   Wave 3: 1.5 ∥ 2.4 ∥ 2.5 → Wave 4: 1.6.
#   Epic 1 and Epic 2 chains are independent and can run in parallel.

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows (respect the gates above)
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
# - Story done ONLY after finish-feature.sh has merged to main and pushed (CLAUDE.md)
# - Off-sprint follow-ups mint v<milestone>-fu<N> keys (one line, status + short description)

generated: 2026-08-05T06:51:14-03:00
last_updated: 2026-08-06
project: imoveis
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # ---- v0.13 pre-story harness track (PRD §6.2 gate — process, not FR) ----
  v0.13-fu1-harness-surgery: done
  # Follow-up: Gemma backfill runner overshot the 16K TPM ceiling at
  # --concurrency 4 (21.15k/16k observed). Added a proactive sliding-60s
  # token-bucket sized from measured per-property token cost (~7k).
  v0.13-fu2-backfill-tpm-limiter: done
  # Follow-up: the backfill could not detect its own completion — `remaining` was
  # total-minus-enriched, which 494 inactive un-enriched rows kept permanently
  # positive, so the completion branch was dead and finished runs exited 0 via
  # the "no progress" valve. Census-based completion + exit codes/banner, photo
  # gate applied, and an attempt ledger that retires rows that never clear.
  v0.13-fu3-backfill-completion-detection: done
  # Follow-up: independent review of the s1.2 review delta (spec flagged
  # followup_review_recommended). RoutingAIClient lifecycle hardening +
  # dispatch test coverage; 1 finding deferred to 1.3 via deferred-work.md.
  v0.13-fu4-s1.2-followup-review: done
  # Follow-up (DW-3/DW-4): migrate-primary.sh's backfill guard was
  # check-then-act — it probed the `:active` heartbeat once and then ran alembic
  # with nothing holding the window, and a runner sleeping out an RPD window
  # cleared that heartbeat and resumed mid-migration. Added the migration-held
  # key `backfill:gemma:migrating` with set-then-check on both sides.
  v0.13-fu6-migration-backfill-mutual-exclusion: in-progress
  # Follow-up (DW-6): run_backfill renewed its lease only from event sites (per
  # launch-loop iteration, per pause poll, per finished row), so nothing renewed
  # it while a row was in flight — one enrichment slower than the TTL let the
  # lease lapse under a live writer. Added a background timer task renewing at
  # ttl/3 for the whole run, cancelled after the in-flight drain.
  v0.13-fu7-backfill-lease-background-renewer: in-progress
  # Follow-up (DW-7): GeminiClient classified quota only on the HTTP-status
  # path, so a throttle arriving as identical connection resets/timeouts
  # re-raised untagged, the runner took the hard-error branch, and the attempt
  # charged before launch stood — burning backfill.max_attempts on good rows
  # inside one throttle window. Repeated identical transport failures within a
  # configurable recency window of an observed 429 now raise the distinguished
  # quota error; everything else stays a hard error.
  v0.13-fu8-gemini-transport-quota-classification: in-progress

  # ---- v0.13 wave (epics.md 2026-08-05, UX-contract refresh) ----
  # Epic 1: Hybrid Enrichment, Productized (Theme A — FR-27/FR-28/FR-29)
  epic-1: in-progress
  1-1-canonical-enrichment-task-classes-routing-config: done
  1-2-live-pipeline-routes-by-task-class-local-fallback: done
  1-3-backfill-runner-control-core: done
  1-4-enrichment-coverage-telemetry: backlog
  1-5-admin-backfill-control-api: backlog
  1-6-operacoes-backfill-card-coverage-visibility: backlog
  epic-1-retrospective: optional

  # Epic 2: Deal-Intelligence Deepening (Theme B cut — FR-30 + FR-32; FR-31 deferred)
  epic-2: backlog
  2-1-cohort-price-m2-percentiles-pipeline: backlog
  2-2-percentile-badge-and-filter: backlog
  2-3-saved-search-new-match-detection: backlog
  2-4-saved-search-alert-management-ui: backlog
  2-5-detail-side-panel-migration: backlog
  epic-2-retrospective: optional
