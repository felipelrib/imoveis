---
topic: Imoveis deal-tracker frontend — full product UX (DESIGN.md + EXPERIENCE.md)
updated: 2026-08-05T05:58
---

- (decision) Sources confirmed: prds/prd-imoveis-2026-08-05 + epics.md + architecture/architecture-imoveis-2026-07-23. Explicitly excluded: frontend/src code import, readiness report, sprint-change proposal, research doc.
- (decision) Stakes: hobby/personal tool (single user Felipe). Accessibility/polish tuned to personal-use reality, not consumer floor.
- (decision) Working mode: coaching path — walk decisions, creative tools woven in.
- (decision) Form factor resolved: desktop browser only (Felipe's desktop). No mobile/multi-surface requirement.
- (decision) Dark theme: keep — Felipe likes it and wants it retained.
- (event) Brain dump — core job: 'is this listing worth a visit' decided by price range + deal scores + location filters, ESPECIALLY proximity to personal points of interest (church, parents' house, girlfriend's house, and places with easy access to those). Personal-POI proximity is central to his decision.
- (event) Brain dump — pains with current UI: looks 'AI designed', info not elegantly presented; map is buggy (flickering, always resets to same starting point).
- (event) Brain dump — operator observability wants: scraper health (if/how working), AI enrichment status incl. cloud-vs-local and which model, worker issues, ingest counts lately (included/excluded/updated), cross-platform dedupe count (properties seen by two sources + the differences between the two records).
- (event) Brain dump — wants sentiment-dimension filters: neighbourhood safety, needs-reform vs recently-reformed, known-quiet area.
- (event) Brain dump — wants recurrence surfaces: 'any NEW properties matching my previous filters?' and recent-filter recall/recommendation (recent neighbourhoods/property types/price ranges suggested for reuse).
- (decision) Notifications: filter-based notifications wanted ('nice'); PLUS 'new since last visit' as a passive complement covering the un-notified case. Both in scope.
- (decision) Language: pt-BR ONLY for now (i18n infra exists pt-BR/en, but UI experience targets pt-BR; semantic search queries also pt-BR).
- (decision) Compare view: not used, keep minimal/deprioritized. Favourites: wants to use in the future — keep as a real surface. Semantic search: nice-to-have, pt-BR.
- (decision) Idea menu: ALL 8 accepted — score explainability (why this verdict), travel-time POI layer (personal anchors + time bands, not km), price-drop badges in grid, hide/dismiss listings, 'since last visit' front-door panel (new matches/drops/gone), operator health strip on main dashboard (not buried in admin), listing freshness + likely-rented/sold gone status, visit shortlist (favourites graduate to to-visit with notes/verdicts).
- (decision) IA: front door = ONE dashboard — Properties page with 'since last visit' panel + operator health strip on top, score-coloured grid below.
- (decision) IA: grid × map = split view, synced — map bbox filters list, card hover highlights pin (Zillow pattern), serves POI-proximity decisions.
- (decision) Percentile presentation: compact badge on grid cards, plain-language pt-BR sentence in modal ('Mais barato que 75% dos aluguéis na Savassi'). Resolves extract open question #6.
- (decision) BH-first: default region filter = BH/MG; SP/Campinas opportunistic data reachable via explicit region filter. Resolves extract open question #12.
- (override) pt-BR-only UI default overrides NFR-7's 'English default' — both catalogs still maintained (engineering rule intact); flag for future PRD pass.
- (decision) Alert channels: email + in-app (bell/panel) + desktop push. No Telegram. Weekly digest rides email (FR-21). Resolves extract open question #10 (channel side).
- (decision) Coverage-as-SLO: YES — enrichment coverage % joins the front-door health strip (scrapers · backend/model · workers · coverage); detail in Admin. Resolves PRD open Q4 / extract #8.
- (change) Percentile notation REVISED: no statistician notation (P25) anywhere. Plain-language percent framing only, 'top X%' flavor — e.g. badge 'top 25% preço' / sentence 'entre os 25% mais baratos do bairro'. Exact pt-BR copy to be fixed in EXPERIENCE.md microcopy.
- (event) Operator deepening: wants scraper-run BEHAVIORAL analytics, not just up/down — did a run finish too early? how does the current run compare to previous runs (duration, yield)? deviation from historical baseline should itself be an alert that something is off.
- (event) REAL FRUSTRATION (journey grit): property looks good in the system, but clicking through to the source shows it's no longer available — scrapers failed to update it. Stale-availability pain. Feeds gone-status/freshness design + candidate 'verify availability now' single-listing recheck action. New-scope flag for PRD.
- (decision) Stale availability: on-demand 'Verificar disponibilidade' single-listing recheck in modal/shortlist PLUS automatic priority rechecks (e.g. daily) for favourites/visit-shortlist items. UX-driven backend scope — flag for PRD.
- (decision) Scraper anomalies: deviation-from-own-baseline turns health-strip scraper chip amber with a reason string ('OLX: run 5× mais rápido que a mediana'); full run-history comparison table in Admin. No external notification for anomalies.
- (decision) Voice & tone: NEUTRAL & CLEAR — plain professional pt-BR, full sentences where helpful ('3 imóveis baixaram de preço desde ontem'), informative never chatty. No terse-cockpit fragments, no warm-companion personality.
- (decision) Visual direction: HYBRID — warm-craft base (warm charcoal + amber undertones, tactile cards) with editorial-style cards (photo-forward, key info visible right away). REMOVE all progress bars (no ember-bar score fill) and REMOVE the 'acende/morno/esfria' typology entirely. [ASSUMPTION] score reads as editorial-style worded verdict + coloured underline — pending Felipe's confirmation via color-theme mocks.
- (decision) Sentiment filters: seguro/reformado/silencioso are STARTING SUGGESTIONS, not a closed set — filter system must handle an extensible/growing tag vocabulary.
- (event) Felipe raised: (a) header/filter-bar growth as tags+neighbourhoods multiply — needs an overflow/scalability pattern; (b) price-filter semantics when switching aluguel↔venda (orders-of-magnitude different ranges); (c) wants more color options for the warm-craft×editorial hybrid.
- (assumption) Filter-bar scalability pattern (proposed, pending veto): bar shows only ACTIVE filters; bairro + sentiment via searchable typeahead pickers (suggestions inside picker); active chips collapse ('Savassi +3'); hard ceiling 2 wrapped lines then 'Filtros (N)' button → full panel; header height constant by contract; saved searches are the long-term answer to vocabulary growth.
- (assumption) Price filter × listing type (proposed, pending veto): remembered per type — aluguel and venda keep independent ranges/scales/formatting; switching tipo swaps scale + restores that type's last range (mirrors BIN-77 price_type no-cross-type-leakage); tipo='ambos' shows both compact labeled ranges, editable separately.
- (decision) CONFIRMED: score presentation = editorial worded verdicts ('Excelente negócio' / 'Bom preço' / 'Preço de mercado' / 'Acima do mercado') with restrained coloured underline; colour never fills areas. Assumption resolved.
- (event) Round 1 palettes (Brasa/Espresso/Latão/Mata Atlântica/Ardósia) — none picked yet; Felipe asked for a round 2 with red-ish, blue-ish and purple-ish registers.
- (decision) PALETTE PICKED: Meia-noite (round 2, blue bolder) — midnight-blue ground #0e1220, electric azure accent #4da3ff, gold favourite star, warm verdict scale, green price-drop signal. Full 19-role token hexes documented in .working/color-themes-2.html style comments (Meia-noite variation). Applied to the warm-craft × editorial hybrid structure.
- (decision) Key Flows agreed in conversation (J1-J4, protagonist Felipe in different modes): J1 'A checagem da noite' — evening check: since-last-visit → drop card top-25% Savassi → modal (history, verdict explainability, sentiment) → map travel-time to POI → climax: '2 fontes' R$200 price gap becomes negotiation lever + 'is it still real?' availability check → star to shortlist. J2 'O alerta que valeu' — push/email from saved-search new-match → straight into modal → star, ~90s. J3 'O operador desconfiado' — health strip: OLX 26h stale + amber anomaly chip → Admin: run-history vs baseline, worker error, re-trigger scrape, glance backfill card (day 3/6, budget %). J4 'O dia da visita' — Saturday: visit shortlist with notes, map travel-times to sequence visits, post-visit dismiss with note / 'fazer proposta'. Anti-climax the design fights: stale availability (his real frustration).
- (decision) Reviewer Gate: opted IN, all three lenses — rubric walker + adversarial UX + pt-BR microcopy. Dispatch after distillation completes.
- (decision) Triage: filter-bar scalability pattern and per-listing-type price ranges CONFIRMED by Felipe — assumptions resolved to firm contract. Drop-alert threshold: PER SAVED SEARCH, next to its notify toggle. Export UI and auth/API-key management: EXPLICITLY DEFERRED (export stays API/digest-only; auth stays current key gate) — consciously undesigned, noted in spine. Non-blocking leftovers logged, not spine-blocking: keyboard shortcut map unspecified; backfill validate-collision warning presentation form still an open [ASSUMPTION].
- (decision) Review-gate resolutions: (1) map→grid sync becomes EXPLICIT — 'filtrar pela área do mapa' toggle applies bbox, bbox renders as removable filter chip; card↔pin hover sync stays automatic. (2) New-match alerts WAIT FOR ENRICHMENT — fire only when verdict/percentile exist. (3) Since-panel resets on REAL ENGAGEMENT (auto after ~2 min active use, never on a glance; panel states its window 'desde ter, 21h'). (4) Detail = SIDE PANEL, not centered modal — map rail stays visible (J1 travel-time beat works); grid scroll preserved. Plus editorial adoption of all reviewer fixes: three-state recheck w/ own timestamp+cooldown, shortlist-gone first-class alert (default on for starred), gone heuristic gated on successful runs + skip-unchanged bumps last-seen, email = guaranteed channel / push opportunistic, alert batching + digest dedupe, since-panel scope = saved searches + favourites, expected-cadence amber + cold-start 'calibrando baseline' + pinned drift baseline, coverage % = minimum across signal types, dismiss undo toast + descartados view, since-panel drill-in filters, shortlist comparable rows + note/status primitive, 'voltou ao mercado' resurrection state, drop-badge anchor = trailing-90-day max, health-warn gets own amber hex (splits from star gold), badge copy 'entre os 25% mais baratos', 'provavelmente vendido' per type, coleta not run, backfill states pt-BR, percentile filter control = dropdown in Filtros panel, price-history chart + admin components get DESIGN specs, semantic search kept as existing surface (pt-BR), starred = watched (no separate watchlist).
- (decision) Key-screen mock coverage: FOUR mocks — Painel hero (final hybrid form) + detail side panel + Operações + Roteiro de visitas. All other surfaces (Buscas salvas, Alertas, Comparar, filter panel) are spine-only by choice.
- (change) REVISED (Felipe): NO 'Roteiro de visitas' concept. The surface is plain FAVORITOS: track starred properties + their availability. Requirements: (a) filterable/optional HISTORY of unavailable favourites (gone favourites don't vanish — they move to a filterable history view); (b) discard from favourites with OPTIONAL reason (a reason note if he wants one, otherwise plain unstar/remove — no mandatory note); (c) DROP the visit-day framing: no visit sequencing on a map strip, no post-visit status enum (a visitar/visitado/fazer proposta/descartado is dead). Comparable rows + verification stamps + shortlist-gone alert all stay. J4 journey must be rewritten around favourites triage + availability history, not a Saturday visit route. Nav label: Favoritos (not Roteiro de visitas). key-roteiro.html mock to be replaced by key-favoritos.html.
- (event) spines finalized
