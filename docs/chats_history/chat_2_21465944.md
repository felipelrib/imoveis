# # Prompt for Claude Opus: Feature Analysis & Documentation f...

**Data/Hora:** 2026-07-21 17:30:55 | **Chat ID:** `21465944-7984-47a0-b075-14e60190fab6`

---

### 👤 User (17:30:55)

# Prompt for Claude Opus: Feature Analysis & Documentation for Imoveis

**Role:** You are an expert Software Architect and Code Analyst, specializing in Python (FastAPI/Celery) and React/Vite applications.

**Context:**
I am working on a real-estate ingestion pipeline called "Imoveis". 
The stack is: Python FastAPI + Celery, PostGIS (Postgres 15), Redis, React/Vite, and local Ollama models.
I am handing off this codebase to you to analyze what has already been built.

**Goal:**
Your objective is to comprehensively analyze the existing features in this codebase. For each distinct feature you identify (e.g., scraper integrations, deduplication logic, AI enrichment, specific frontend views), I need you to:
1. **Analyze:** Understand how it was implemented and how it currently works under the hood.
2. **Review for Bugs:** Thoroughly review the implementation for any logic flaws, edge cases, security issues, performance bottlenecks, or bugs.
3. **Document:** Write comprehensive feature documentation for *each feature* strictly following the provided `_template.md` (shown below). Save these documents into the `docs/features/` directory.

---

## Required Template (`docs/features/_template.md`)

```markdown
# <feature-name> — <one-line description>

> Feature branch: `feat/<slug>` · Linear: `BIN-XX` · Status: implemented

## Problem

What user pain or technical gap does this feature address?

## Approach

- Bullet points describing the high-level design decisions.
- Why this approach was chosen over alternatives.

## Changes

Files touched:

```
 path/to/file.py       | WHAT CHANGED — short description
 path/to/other/file.py | NEW — new file description
```

## New Dependencies

Any new packages added to `requirements.txt` or `package.json`? Or "None".

## How to Test

1. Steps to manually verify the feature works.
2. Or the test command:
   ```bash
   bash scripts/agent/validate.sh backend
   ```

## Notes / Follow-ups

- Any known limitations, tech debt, or follow-up work.
- Links to related Linear issues.
```

*(Note for the AI: When filling out "Files touched", list the primary files that constitute the feature. If you find bugs or tech debt, list them heavily under "Notes / Follow-ups")*

---

## Project Context

### 1. Architecture Overview
```text
src/
├── api/                          # FastAPI routers, admin endpoints
├── adapters/                     # External integrations
│   ├── db/                       # SQLAlchemy ORM models
│   ├── scrapers/                 # Platform scrapers (add-on pattern)
│   ├── ai/                       # LocalAIClient abstraction (Ollama, LM Studio)
│   ├── queue/                    # Celery tasks + GPU semaphore
│   └── metrics/                  # Statistical scoring
├── core/                         # Business logic (dedup, entities)
├── infra/                        # Config, DB, Redis, logging
└── tests/                        # pytest suite (unit + integration)
frontend/                         # React 18 + Vite
configs/app_config.yaml           # Single source of truth for all settings
scripts/                          # Project management scripts
```
**Data flow:** Scraper → Normalize → Dedupe → DB → Metrics → AI Enrich

### 2. General Day-to-Day Commands
- `./scripts/start.sh` — Start the stack (builds if needed, runs migrations)
- `./scripts/test.sh all` — Run tests (`unit`, `integration`, `e2e`, or `all`)
- `./scripts/dev.sh` — Start backend + frontend dev server (hot-reload)

### 3. Key Development Rules (For your awareness during review)
- **Database:** SQLAlchemy 2.0 style (`mapped_column`). Always parameterized queries. Migrations via Alembic.
- **Backend:** FastAPI with Pydantic v2 patterns (`model_validate()`, `model_dump()`). Async route handlers. Structured logging via `structlog` (No `print()`). Config via `AppConfig` from YAML + env variables.
- **Frontend:** Hooks-first React design. All API calls through a shared API module, not raw `fetch()` in components. Loading and error states must be explicitly handled. No `dangerouslySetInnerHTML` without DOMPurify.

---

**Your Task:**
1. Start by scanning the `src/` and `frontend/` directories to identify the major features currently implemented.
2. For each identified feature, analyze the code deeply. Look for architectural anti-patterns, potential race conditions (especially in Celery tasks or DB transactions), unhandled exceptions, or missing loading states in the frontend.
3. Generate the markdown files based exactly on the `_template.md` format and place them in the `docs/features/` folder.
4. Conclude by giving me a summary report of any critical bugs or technical debt you discovered during your analysis.

Notice that you already did analyze the features from 01 (@[/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/01-scraper-framework.md] ) to 10 (@[/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/10-favourites-and-saved-searches.md]), so skip those

<ADDITIONAL_METADATA>
The current local time is: 2026-07-21T14:30:55-03:00.

The user's current state is as follows:
Active Document: /home/felipe/workfolder/imoveis/docs/features/10-favourites-and-saved-searches.md (LANGUAGE_MARKDOWN)
Cursor is on line: 65
Other open documents:
- /home/felipe/workfolder/imoveis/.cline/skills/feature-pipeline/SKILL.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/.cline/skills/code-review/SKILL.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/08-frontend-dashboard.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/07-rest-api.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/05-price-drop-alerts.md (LANGUAGE_MARKDOWN)

The user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:

@[/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/01-scraper-framework.md] is a [File]:
/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/01-scraper-framework.md
@[/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/10-favourites-and-saved-searches.md] is a [File]:
/wsl+ubuntu/home/felipe/workfolder/imoveis/docs/features/10-favourites-and-saved-searches.md
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Claude Sonnet 4.6 (Thinking). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

### 👤 Assistant (17:31:26)

Now let me read the key files to understand the features not yet documented:

---

### 👤 Assistant (17:32:16)

Now I have a thorough understanding of the codebase. Let me look at two more files to check the existing docs I should be aware of (the ones that are not numbered 01-10 yet, which appear to be draft/incomplete versions):

---

### 👤 Assistant (17:32:35)

Now I have a complete picture of all features and the existing docs. The unnumbered docs (config-yaml-loader, deal-summary-enrichment, interactive-map, per-platform-listings, saved-searches-favourites) are already documented but in non-standard format. I need to create proper numbered docs (11 onwards) in the exact `_template.md` format for all features beyond 10 that aren't yet properly documented. Let me also look at a few remaining files:

---

### 👤 Assistant (17:32:45)

I now have everything I need to write the feature docs. Let me now create all the numbered docs (11-16) for the features not yet covered by 01-10:

1. **11** — Config YAML Loader (already has an unnumbered draft)
2. **12** — Circuit Breaker & Checkpoint (new — not documented)
3. **13** — Image Store & VLM Pipeline (new — not documented)
4. **14** — Interactive Map (already has a draft)
5. **15** — Deal Summary Enrichment (already has a draft)
6. **16** — Per-Platform Listings (already has a draft)
7. **17** — Watchlist (partially in feature 10 but distinct — `src/api/watchlist.py` is separate from favourites/saved-searches)
8. **18** — Admin Control Panel (new — not documented)
9. **19** — System Status & Pipeline Telemetry (new — not documented)

---

### 👤 Assistant (17:32:53)

Good. Feature 10 already covers Favourites and Saved Searches fully. Now I'll look at what the remaining unnumbered docs cover vs what new features need docs, and write the new documents. The features I need to document (11-16) are:

- **11**: Config YAML Loader (standardize the existing draft)
- **12**: Circuit Breaker & Scraper Checkpoint Store
- **13**: Image Store & VLM Image Pipeline
- **14**: Interactive Map View
- **15**: Deal Summary Enrichment
- **16**: Per-Platform Listings with Price Comparison
- **17**: Watchlist & Price-Drop Alert Notifications
- **18**: Admin Control Panel (worker pause/GPU scaling/scoring recalculate/schedule mgmt)
- **19**: System Status & Pipeline Telemetry

---

### 👤 Assistant (17:38:06)

Now let me create the summary report artifact:

---

### 👤 Assistant (17:39:02)

All 9 documentation files are written. Here's a summary of everything produced:

---

## Documentation Written (Features 11–19)

| # | File | Feature |
|---|------|---------|
| 11 | [11-config-yaml-loader.md](file:///home/felipe/workfolder/imoveis/docs/features/11-config-yaml-loader.md) | Pydantic v2 YAML config + env overrides |
| 12 | [12-circuit-breaker-and-checkpoint.md](file:///home/felipe/workfolder/imoveis/docs/features/12-circuit-breaker-and-checkpoint.md) | Distributed Redis circuit breaker + DB checkpoint store |
| 13 | [13-image-store-and-vlm-pipeline.md](file:///home/felipe/workfolder/imoveis/docs/features/13-image-store-and-vlm-pipeline.md) | MD5-deduped local image cache + VLM visual analysis |
| 14 | [14-interactive-map.md](file:///home/felipe/workfolder/imoveis/docs/features/14-interactive-map.md) | MapLibre GL map with clustering, score colours, bbox filter |
| 15 | [15-deal-summary-enrichment.md](file:///home/felipe/workfolder/imoveis/docs/features/15-deal-summary-enrichment.md) | LLM deal verdict with deterministic PT-BR template fallback |
| 16 | [16-per-platform-listings.md](file:///home/felipe/workfolder/imoveis/docs/features/16-per-platform-listings.md) | Cross-platform price comparison on cards & modal |
| 17 | [17-watchlist-price-drop-alerts.md](file:///home/felipe/workfolder/imoveis/docs/features/17-watchlist-price-drop-alerts.md) | Per-property watchlist + pluggable notifier channels |
| 18 | [18-admin-control-panel.md](file:///home/felipe/workfolder/imoveis/docs/features/18-admin-control-panel.md) | Auth'd admin API: workers, GPU scaling, scoring, schedules |
| 19 | [19-system-status-and-pipeline-telemetry.md](file:///home/felipe/workfolder/imoveis/docs/features/19-system-status-and-pipeline-telemetry.md) | Live health check + AI throughput telemetry |

---

## Critical Bugs Summary

The full report is in the [feature_analysis_report.md](file:///home/felipe/.gemini/antigravity-ide/brain/21465944-7984-47a0-b075-14e60190fab6/feature_analysis_report.md) artifact. Top items to fix immediately:

1. **🔴 CRITICAL — `await` outside event loop in `ai_enrich`** ([tasks.py ~L317](file:///home/felipe/workfolder/imoveis/src/adapters/queue/tasks.py#L317-L323)): `await client.summarize_deal(...)` is called in a sync Celery task without an `asyncio.run()` wrapper — crashes every enrichment task that generates a deal verdict.

2. **🔴 CRITICAL — XSS in MapLibre popup** ([MapView.jsx ~L149](file:///home/felipe/workfolder/imoveis/frontend/src/components/MapView.jsx#L134-L149)): `setHTML()` with unsanitised database content. Use DOMPurify or `setDOMContent()`.

3. **🟠 HIGH — Watchlist alert loop is missing** ([watchlist.py](file:///home/felipe/workfolder/imoveis/src/api/watchlist.py)): The entire notifier infrastructure exists but no task ever evaluates watchlist rows — no alerts are ever sent.

4. **🟠 HIGH — Blocking `httpx.get` in async FastAPI route** ([system.py L49](file:///home/felipe/workfolder/imoveis/src/api/system.py#L46-L53)): Blocks the event loop for up to 3 seconds per `/system/status` call.

5. **🟠 HIGH — Neighbourhood UUID sent to LLM instead of name** ([tasks.py ~L311](file:///home/felipe/workfolder/imoveis/src/adapters/queue/tasks.py#L308-L315)): Deal verdict prompt receives `"a3f2c..."` instead of `"Savassi"`.

---

### 👤 User (17:44:33)

Remove the old docs for features (or rewrite them if they don't exist), adjust the Cline skills/rules to write features following the existing template

<ADDITIONAL_METADATA>
The current local time is: 2026-07-21T14:44:33-03:00.

The user's current state is as follows:
Active Document: /home/felipe/workfolder/imoveis/docs/features/10-favourites-and-saved-searches.md (LANGUAGE_MARKDOWN)
Cursor is on line: 65
Other open documents:
- /home/felipe/workfolder/imoveis/docs/features/13-image-store-and-vlm-pipeline.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/16-per-platform-listings.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/14-interactive-map.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/18-admin-control-panel.md (LANGUAGE_MARKDOWN)
- /home/felipe/workfolder/imoveis/docs/features/15-deal-summary-enrichment.md (LANGUAGE_MARKDOWN)
</ADDITIONAL_METADATA>

---

### 👤 Assistant (17:44:57)

Now I have a complete picture. The plan is:

1. **Delete** the 5 old unnumbered docs (config-yaml-loader.md, deal-summary-enrichment.md, interactive-map.md, per-platform-listings.md, saved-searches-favourites.md)
2. **Update** `.cline/skills/feature-pipeline/SKILL.md` — replace the `gen-docs.sh` step with explicit instructions to write a properly numbered doc using `docs/features/_template.md`
3. **Update** `.clinerules/rules.md` — add a "Feature Documentation" section with the exact template and numbering convention

---

### 👤 Assistant (17:54:56)

Clean. Now update the feature-pipeline skill to replace the vague `gen-docs.sh` step with an explicit, template-driven documentation step:

---

### 👤 Assistant (17:55:21)

Now add a "Feature Documentation" section to the master `.clinerules/rules.md`:

---

### 👤 Assistant (17:55:43)

Let me do a quick sanity check on the final state of both files:

---

### 👤 Assistant (18:29:44)

Everything looks exactly right. Here's a summary of all changes made:

---

## What was done

### 1. Deleted 5 old unnumbered feature docs
These freeform, inconsistently formatted files were removed:

| Deleted |
|---------|
| `docs/features/config-yaml-loader.md` |
| `docs/features/deal-summary-enrichment.md` |
| `docs/features/interactive-map.md` |
| `docs/features/per-platform-listings.md` |
| `docs/features/saved-searches-favourites.md` |

`docs/features/` is now a clean, contiguous numbered sequence: `01` → `19`.

---

### 2. Updated [feature-pipeline/SKILL.md](file:///home/felipe/workfolder/imoveis/.cline/skills/feature-pipeline/SKILL.md)

**Step 10** was replaced entirely. The old vague `bash scripts/agent/gen-docs.sh` call became a three-sub-step process:
- **10a**: determine the next sequential number from the filesystem
- **10b**: create `<NN>-<slug>.md` using `_template.md` verbatim, with explicit rules for each section and a `**BUG (Severity)**` format for bugs
- **10c**: commit with conventional message `docs: <NN>-<slug> feature doc`

---

### 3. Updated [.clinerules/rules.md](file:///home/felipe/workfolder/imoveis/.clinerules/rules.md)

Added a **`## Feature Documentation (NON-NEGOTIABLE)`** section under Conventions, covering:
- Naming convention (`<NN>-<slug>.md`, sequential, zero-padded)
- The shell command to determine the next number
- All 8 required template sections with guidance
- Hard bans: no freeform docs, no unnumbered files, no omitted sections, bugs go in the doc only

---
