# # Prompt for Claude Opus: Optimizing AI Harness for Imoveis ...

**Data/Hora:** 2026-07-20 21:51:42 | **Chat ID:** `999a451f-0f3b-4e29-a085-caba36912d7c`

---

### 👤 User (21:51:42)

# Prompt for Claude Opus: Optimizing AI Harness for Imoveis

**Role:** You are an expert AI development architect and maintainer, specializing in autonomous AI coding agents (specifically Cline).

**Context:**
I am developing a real-estate ingestion pipeline called "Imoveis". The stack is Python FastAPI + Celery, PostGIS, Redis, React/Vite, and local Ollama models.
I develop this project entirely using **Cline**. I use a dual-model strategy: a strong model (like DeepSeek v4 pro or Claude 3.5 Sonnet) for planning, and a cheaper/lower-context model for executing the plan.
My project is managed via Linear for tasks.

**Goal:**
I want you to overhaul, clean up, and perfect my Cline harness and project repository to provide the absolute best AI development experience. Specifically, I need you to:
1. **Optimize the Cline Rules & Skills:** Analyze my existing `.clinerules/` and `.cline/` directories (provided below). Clean them up, remove redundancies, and ensure they perfectly align with Cline's standard capabilities (as documented in https://docs.cline.bot/customization/cline-rules). The rules must explicitly instruct the AI on how to handle Linear tasks, validate implementations, verify CI passes, and update docs/Linear.
2. **Clean up the Repo:** Remove any files, rules, or documentation that do not make sense to live in the git repository (e.g., things that should just be in Linear or are obsolete).
3. **Update Documentation & README:** Rewrite the `README.md` to clearly explain how the project works, its features, and the architecture. Establish a structure for "internal docs" (e.g., in `docs/features/`) detailing how features were implemented, architecture decisions, and the "whys", and link these appropriately from the README.

**Important Considerations for the Dual-Model Setup:**
- Since I use a strong model for planning and a weaker model for implementation, the rules must enforce that the planner writes crystal-clear, heavily detailed `implementation_plan.md` files. The implementer model must be strictly instructed to follow the plan, run validation scripts, and not diverge into architectural changes.

---

## Project Context Provided:

### 1. Current README.md
```markdown
# Imoveis — Deal Tracker

Real-estate ingestion pipeline. Multi-platform scraping, heuristic deduplication,
PostGIS-backed geospatial storage, local AI enrichment, and price tracking.

**Stack:** Python FastAPI + Celery · PostGIS (Postgres 15) · Redis · React/Vite · Ollama

## Quick Start

```bash
git clone https://github.com/felipelrib/imoveis.git
cd imoveis
./scripts/setup.sh          # builds images, starts stack, runs migrations
```

Then open:
- **Frontend:** http://localhost:5173
- **API:** http://localhost:8000
- **API docs:** http://localhost:8000/docs

## Day-to-Day Commands

| Script | What it does |
|--------|-------------|
| `./scripts/start.sh` | Start the stack (builds if needed, runs migrations) |
| `./scripts/stop.sh` | Stop containers (volumes preserved) |
| `./scripts/restart.sh` | Stop + start (`--build` to rebuild images) |
| `./scripts/test.sh` | Run tests (`unit`, `integration`, `e2e`, or `all`) |
| `./scripts/dev.sh` | Start backend + frontend dev server (hot-reload) |
| `./scripts/clean.sh` | Tear down + remove volumes (`--all` removes images too) |

## Architecture

```
src/
├── api/                          # FastAPI routers, admin endpoints
├── adapters/                     # External integrations
│   ├── db/                       # SQLAlchemy ORM models
│   ├── scrapers/                 # Platform scrapers (add-on pattern)
│   ├── ai/                       # LocalAIClient abstraction (Ollama, LM Studio)
│   ├── queue/                    # Celery tasks + GPU semaphore
│   └── metrics/                  # Statistical scoring
├── core/                         # Business logic (dedup, entities)
├── infra/                        # Config, DB, Redis, logging
└── tests/                        # pytest suite (unit + integration)
frontend/                         # React 18 + Vite
configs/app_config.yaml           # Single source of truth for all settings
scripts/                          # Project management scripts (see table above)
```
```

### 2. Current Rules (.clinerules/04-imoveis-specific.md)
*Note: This is the most project-specific rule file currently in use.*
```markdown
# IMOVEIS-SPECIFIC RULES

## Workflow
3. PLAN BEFORE CODE. An `implementation_plan.md` must exist before you write any implementation code. Follow the plan. The plan MUST include a **testing strategy** section.
4. COMMIT FREQUENTLY to your feature branch with conventional messages.
5. VALIDATE before declaring done. NEVER run raw `pytest` or `npm test`. ALWAYS use:
   `bash scripts/agent/validate.sh fast` or `backend` or `all`.
   When the feature is complete, use `bash scripts/agent/finish-feature.sh --pr`.

## Skill usage
13. When the user says "work on the next ticket", your FIRST action MUST be `use_skill(skill_name="feature-pipeline")`. Do NOT manually replicate the skill's steps.

## Validation discipline (NON-NEGOTIABLE)
14. NEVER skip, work around, or ignore validation failures. If `validate.sh`, `finish-feature.sh`, `npm ci`, `pytest`, `isort`, `flake8` fails, you MUST diagnose, fix, and re-run.

## Feature workflow
Features are tracked in **Linear** (team "Bino"). Use `linear_search_issues` MCP tool to find the next issue to work on.
1. **Plan.** Read the Linear issue via MCP, then write `implementation_plan.md`.
2. **Isolate workspace.** `bash scripts/agent/setup-branch.sh <feature-slug>`
3. **Run services.** `docker-compose up -d`
4. **Implement + commit.** Small conventional commits.
5. **Validate locally.** `bash scripts/agent/validate.sh all`
6. **Open PR and wait for CI gate.** Use `bash scripts/agent/finish-feature.sh --pr`.
7. **Update Linear.** Set the issue status to Done via Linear MCP.
8. **Document.** Generate feature docs via `gen-docs.sh`.
```

### 3. Current Custom Skills (.cline/skills/)
- `code-review`
- `feature-pipeline`
- `finish-feature`
- `security-scan`
- `validate-feature`

### 4. Current Rule Files (.clinerules/)
- `01-universal.md`
- `02-python-backend.md`
- `03-react-frontend.md`
- `04-imoveis-specific.md`
- `ci.md`
- `guardrails.md`
- `project.md`
- `session-workflow.md`
- `testing.md`

---

**Your Task:**
Please provide the exact files I should add/modify/delete in my repository, the exact contents of the optimized `.clinerules`, and the new structure for my `README.md` and `docs/features/`. Make sure the instructions are optimized for a Planner (DeepSeek v4 pro) + Implementer (cheap model) dual-agent flow, leveraging Linear MCP tools for task management, and keeping repo clutter to an absolute minimum.


<ADDITIONAL_METADATA>
The current local time is: 2026-07-20T18:51:42-03:00.

The user's current state is as follows:
Active Document: /home/felipe/workfolder/imoveis/README.md (LANGUAGE_MARKDOWN)
Cursor is on line: 1
Other open documents:
- /home/felipe/workfolder/imoveis/README.md (LANGUAGE_MARKDOWN)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Claude Opus 4.6 (Thinking). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

### 👤 Assistant (21:51:42)

This is a significant overhaul that warrants thorough research before planning. Let me start by examining the existing project structure and Cline documentation.

---

### 👤 Assistant (21:51:58)

Let me dig deeper into all the existing files.

---

### 👤 Assistant (21:52:08)

Now let me look at the skills, kanban, and other supporting files:

---

### 👤 Assistant (21:52:17)

Now let me read the skills content:

---

### 👤 Assistant (21:52:32)

Now let me read the existing docs and check for remaining items to assess:

---

### 👤 Assistant (21:52:41)

Now let me also check Cline's docs page for the markdown version to get useful info about the rules format:

---

### 👤 Assistant (21:52:54)

Excellent — I now have a thorough understanding of the current state. Let me also quickly check what stale files exist at the root:

---

### 👤 Assistant (21:53:02)

I now have a comprehensive picture. Let me create the implementation plan.

---

### 👤 Assistant (21:54:13)

The implementation plan is ready for your review. Key highlights:

- **Rules: 9 → 4 files** (~60% token reduction) using Cline's conditional `paths:` frontmatter to only activate backend/frontend/testing rules when relevant files are in context
- **Skills: 5 → 3** — removing `validate-feature` (thin wrapper) and `finish-feature` (subset of `feature-pipeline`)
- **Repo cleanup** — removing `qa_data.json`, logs, and the stale root `implementation_plan.md`
- **Dual-model optimization** — the new `rules.md` explicitly defines Planner vs Implementer roles and behavioral constraints
- **New docs structure** — ADR directory + feature doc template

Please review the open questions and confirm items before I proceed with implementation.

---

### 👤 User (21:54:59)

Comments on artifact URI: file:///home/felipe/.gemini/antigravity-ide/brain/999a451f-0f3b-4e29-a085-caba36912d7c/implementation_plan.md

The user has approved this document.





<ADDITIONAL_METADATA>
The current local time is: 2026-07-20T18:54:59-03:00.

The user's current state is as follows:
Active Document: /home/felipe/workfolder/imoveis/README.md (LANGUAGE_MARKDOWN)
Cursor is on line: 1
Other open documents:
- /home/felipe/workfolder/imoveis/README.md (LANGUAGE_MARKDOWN)
</ADDITIONAL_METADATA>

---

### 👤 Assistant (21:54:59)

Plan approved — let me start executing. First, I'll create the task tracker.

---

### 👤 Assistant (21:55:18)

Starting Phase 1 — Repo Cleanup:

---

### 👤 Assistant (21:55:33)

Phase 1 done. Now Phase 2 — Creating the new consolidated rules. Starting with the master `rules.md`:

---

### 👤 Assistant (21:56:35)

Now the conditional backend rules:

---

### 👤 Assistant (21:57:37)

Now delete the old rule files:

---

### 👤 Assistant (21:57:44)

Phase 2 done. Now Phase 3 — Skills. Rewrite `feature-pipeline`:

---

### 👤 Assistant (21:58:11)

Now update `code-review` skill (remove raw pytest reference):

---

### 👤 Assistant (21:58:33)

Now delete the redundant skills:

---

### 👤 Assistant (21:58:38)

Phase 3 done. Now Phase 4 — README, docs, and MkDocs:

---

### 👤 Assistant (21:59:14)

Now create the feature template and ADR:

---

### 👤 Assistant (21:59:46)

Now update MkDocs nav:

---

### 👤 Assistant (21:59:58)

Phase 4 done. Now Phase 5 — Verification. Let me check the token savings and verify the final state:

---

### 👤 Assistant (22:00:06)

The "failed" exit code is just because `ls` on deleted files returned errors (which confirms they're deleted). Let me get a clean comparison:

---

### 👤 Assistant (22:00:14)

The `bc` calculation had a rounding issue — let me compute properly: 14,701 / 37,803 = ~39%, so **reduction is ~61%**. Now let me update the task list and create the walkthrough:

---

### 👤 Assistant (22:01:00)

All done. Here's the summary:

## What Changed

**Repo Cleanup** — Deleted 4 stale files (~500KB), added `implementation_plan.md` to `.gitignore`.

**Rules: 9 → 4 files (61% token reduction)** — The always-on [rules.md](file:///home/felipe/workfolder/imoveis/.clinerules/rules.md) covers dual-model protocol, session lifecycle, validation, Linear integration, and safety. Three conditional files ([python-backend.md](file:///home/felipe/workfolder/imoveis/.clinerules/python-backend.md), [react-frontend.md](file:///home/felipe/workfolder/imoveis/.clinerules/react-frontend.md), [testing.md](file:///home/felipe/workfolder/imoveis/.clinerules/testing.md)) only activate when touching relevant files.

**Skills: 5 → 3** — [feature-pipeline](file:///home/felipe/workfolder/imoveis/.cline/skills/feature-pipeline/SKILL.md) rewritten with explicit Planner/Implementer phases. Removed `validate-feature` and `finish-feature` (redundant wrappers).

**README** — [Complete rewrite](file:///home/felipe/workfolder/imoveis/README.md) with architecture, quick start, commands, and documentation links.

**Docs** — Added [feature template](file:///home/felipe/workfolder/imoveis/docs/features/_template.md), [ADR-0001](file:///home/felipe/workfolder/imoveis/docs/adr/0001-dual-model-development.md), and updated [mkdocs.yml](file:///home/felipe/workfolder/imoveis/mkdocs.yml).

See the full walkthrough artifact for details.

---
